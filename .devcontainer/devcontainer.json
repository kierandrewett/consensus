{
    "name": "Consensus Voting System",
    "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
    "postCreateCommand": "yarn install && yarn build && yarn db:reset && yarn db:seed",
    "remoteUser": "root",
    "forwardPorts": [3000],
    "customizations": {
        "vscode": {
            "extensions": [
                "dbaeumer.vscode-eslint",
                "esbenp.prettier-vscode"
            ]
        }
    }
}
