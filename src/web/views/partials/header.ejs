<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Consensus</title>
    <link rel="icon" type="image/x-icon" href="/public/favicon.ico">
    <link rel="icon" type="image/svg+xml" href="/public/favicon.svg">
    <link rel="stylesheet" href="/public/css/styles.css">
    <script defer src="/public/js/mts.js"></script>
</head>
<body>
    <%- include('banner') %>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="brand">
                <i data-lucide="vote" style="width: 24px; height: 24px;"></i>
                <span>Consensus</span>
            </a>
            <div class="nav-links">
                <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                    <a href="/admin/dashboard" class="nav-link">Dashboard</a>
                    <span class="nav-user"><%= adminUsername %></span>
                    <a href="/admin/logout" class="nav-link nav-link-muted">Logout</a>
                <% } else if (typeof voterID !== 'undefined' && voterID) { %>
                    <a href="/dashboard" class="nav-link">Dashboard</a>
                    <span class="nav-user"><%= voterName %></span>
                    <a href="/logout" class="nav-link nav-link-muted">Logout</a>
                <% } else { %>
                    <a href="/elections" class="nav-link">Browse Elections</a>
                    <span class="nav-divider">|</span>
                    <a href="/login" class="nav-link">Sign In</a>
                    <a href="/register" class="btn btn-primary btn-sm">Register</a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container">