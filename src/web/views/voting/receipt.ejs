<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vote Receipt - <%= election.name %></title>
    <link rel="icon" type="image/x-icon" href="/public/favicon.ico">
    <link rel="icon" type="image/svg+xml" href="/public/favicon.svg">
    <link rel="stylesheet" href="/public/css/styles.css">
</head>
<body class="receipt-page">
    <div class="receipt-header">
        <div class="checkmark">✓</div>
        <h1>Vote Receipt</h1>
        <div class="system-name">Consensus E-Voting System</div>
    </div>
    
    <div class="voter-info">
        <div class="voter-info-header">Voter Information</div>
        <% if (typeof isGuest !== 'undefined' && isGuest) { %>
        <div class="voter-info-row">
            <span class="voter-info-label">Voter Type</span>
            <span class="voter-info-value">Guest Voter</span>
        </div>
        <% } else { %>
        <div class="voter-info-row">
            <span class="voter-info-label">Voter ID</span>
            <span class="voter-info-value"><%= voterID %></span>
        </div>
        <div class="voter-info-row">
            <span class="voter-info-label">Voter Name</span>
            <span class="voter-info-value"><%= voterName %></span>
        </div>
        <% } %>
    </div>
    
    <div class="detail">
        <div class="label">Election</div>
        <div class="value"><%= election.name %></div>
    </div>
    
    <div class="detail">
        <div class="label">Confirmation ID</div>
        <div class="value mono"><%= confirmation.confirmationID %></div>
    </div>
    
    <div class="detail">
        <div class="label">Date &amp; Time</div>
        <div class="value"><%= confirmation.confirmedAt.toLocaleString() %></div>
    </div>
    
    <div class="privacy-note">
        <strong>Privacy Notice</strong>
        This receipt confirms your vote was recorded. Your vote choice remains anonymous and confidential.
    </div>
    
    <div class="receipt-footer">
        <p>Thank you for participating in the democratic process.</p>
        <p>Keep this receipt for your records.</p>
    </div>
    
    <div class="no-print">
        <button class="btn" onclick="window.print()">Print Receipt</button>
        <button class="btn btn-secondary" onclick="window.close()">Close</button>
    </div>
    
    <script>
        // Auto-print when opened
        window.onload = function() {
            window.print();
        };
    </script>
</body>
</html>
