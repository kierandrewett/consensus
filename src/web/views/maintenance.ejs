<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Consensus</title>
    <link rel="icon" type="image/x-icon" href="/public/favicon.ico">
    <link rel="icon" type="image/svg+xml" href="/public/favicon.svg">
    <link rel="stylesheet" href="/public/css/styles.css">
    <style>
        .maintenance-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 2rem;
            text-align: center;
            background: var(--bg);
        }
        .maintenance-icon {
            width: 80px;
            height: 80px;
            color: var(--muted);
            margin-bottom: 1.5rem;
        }
        .maintenance-title {
            font-size: 2rem;
            font-weight: 600;
            color: var(--text);
            margin-bottom: 1rem;
        }
        .maintenance-message {
            font-size: 1.125rem;
            color: var(--muted);
            max-width: 500px;
            line-height: 1.6;
        }
        .maintenance-brand {
            margin-top: 3rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--muted);
            font-size: 0.875rem;
        }
        .maintenance-brand i {
            width: 20px;
            height: 20px;
        }
    </style>
</head>
<body>
    <%- include('partials/banner') %>
    <div class="maintenance-container">
        <svg class="maintenance-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/>
        </svg>
        <h1 class="maintenance-title">Under Maintenance</h1>
        <p class="maintenance-message"><%= message %></p>
        <a href="/admin/login" style="margin-top: 1.5rem; font-size: 0.875rem; color: var(--muted); text-decoration: none;">Admin Login</a>
        <div class="maintenance-brand">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 12 2 2 4-4"/><path d="M5 7c0-1.1.9-2 2-2h10a2 2 0 0 1 2 2v12H5V7Z"/><path d="M22 19H2"/></svg>
            <span>Consensus Voting System</span>
        </div>
    </div>
</body>
</html>
